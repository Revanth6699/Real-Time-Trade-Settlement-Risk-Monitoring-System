global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: trade-risk-api

    scheme: https

    metrics_path: /metrics

    static_configs:
      - targets:
          - real-time-trade-settlement-risk.onrender.com